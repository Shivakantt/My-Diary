<%- include('partial/header.ejs'); %>
  <div class="container my-4">
    <h2>Home</h2>
    <p>
      <%= content %>
    </p>
    
    
    <% posts.forEach(function(post){%>
     <h2>  <%= post.title%></h2> 
     <p>  <%= post.content.substring(0,100)+" ..." %> <a href="/posts/<%= post.title %>">Read me</a></p>
    <% } ); %> 
  </div>
 
<%- include('partial/footer.ejs'); %>